{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "xxx: kill running app",
            "type": "shell",
            "command": "pkill -f xxx || true",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        {
            "label": "xxx: build",
            "type": "shell",
            "command": "./Scripts/build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "dependsOn": "xxx: kill running app",
            "dependsOrder": "sequence",
            "problemMatcher": []
        }
    ]
}
