{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run xxx (Debug)",
            "type": "node-terminal",
            "request": "launch",
            "preLaunchTask": "xxx: build",
            "command": "${workspaceFolder}/DerivedData/Build/Products/Debug/xxx.app/Contents/MacOS/xxx",
            "cwd": "${workspaceFolder}",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
